#ifndef DATA_H
#define DATA_H

/*
so the XmlParser will parse out the XmlData objects data into the Data object,
which will sort the data and then the XmlParser will pass off the Data object to the DataManager object
*/

class Data
{
    public:
        Data();
        virtual ~Data();

    protected:

    private:
};

#endif // DATA_H


